{
  "name": "kabadiman",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "admin:dev": "turbo run dev --filter=admin-panel",
    "customer:start": "turbo run start --filter=customer-app",
    "partner:start": "turbo run start --filter=partner-app"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "packageManager": "pnpm@9.15.0"
}
